RewriteEngine On
ErrorDocument 404 /404.php

# 1. Force HTTPS
RewriteCond %{HTTP:X-Forwarded-Proto} !https
RewriteCond %{HTTPS} off
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# 2. Remove .php extension (Redirect external request /file.php to /file)
RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s([^.]+)\.php [NC]
RewriteRule ^ %1 [R=301,L]

# Redirect old /calculadora page to homepage section
RewriteRule ^calculadora/?$ /#calculadora-home [R=301,L,NE]

# 3. Handle extensionless URLs (Internally rewrite /file to /file.php)
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^([^\.]+)$ $1.php [NC,L]
